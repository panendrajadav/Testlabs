llm:
  provider: azure
  model: gpt-4o
  temperature: 0.3
  azure_endpoint: ${AZURE_OPENAI_ENDPOINT}
  api_version: 2024-05-01-preview

automl:
  task_type: auto
  cv_folds: 3
  score_threshold: 0.99
  max_iterations: 1

optuna:
  n_trials: 3
  direction: maximize
